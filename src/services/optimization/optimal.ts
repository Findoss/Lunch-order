import type { Order, OrderCombo } from '../../models/order/types';

export const selectOptimalCombo = (rawCombos: OrderCombo[]) => {
  const combos: OrderCombo[] = [];

  console.log(combos);

  // сортируем самый выгодный
  // выбираем
  // удаляем юзеров
  // повторяем
  return combos;
};
